/******************************************************************************

                            Online C Compiler.
                Code, Compile, Run and Debug C program online.
Write your code in this editor and press "Run" button to compile and execute it.

*******************************************************************************/
#include <stdbool.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
int main()
{
    int typ =3;
    char wiad[1024]="!k 201 50";
	char newmsg[20] = "";
	bool flagwiad = false;
	char korektor[] = " ";
	char *schowek;
	bool i = false;
	char podflaga[100];
	int licznik = 0;
	
	schowek = strtok (wiad, korektor);

	while (schowek != NULL){
		if (strchr(schowek, '!') && i == false){
			  flagwiad = true;
		}
		if (i == true && flagwiad == true){
			if(typ ==1 ){
				podflaga[0] = schowek[0]; //przypisanie podfladze litery od ktorych sie maja zaczynac slowa
			}
			if(typ ==2 || typ==3){
			    for(int j=0;j<strlen(schowek);j++){
				    podflaga[licznik] += schowek[j]; // przypisanie podfladze znaku <+/-> i liczby dlugosci LUB liczby i liczby
				    licznik++;
			    }
			   
			}
			
			 podflaga[strlen(schowek)+1]+='.';
			
		
		
		}
		else if(flagwiad==false){
			printf (" twoja wiadomosc byla niepoprawna\n");
			break;
		}
			
		schowek = strtok (NULL, korektor);
		i = true;
	}
        printf("%s", podflaga);
}
